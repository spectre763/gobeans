# GoBeans Public Assets

This directory contains static assets served by Next.js.

## /sequence/

The image sequence frames live here:
- `ezgif-frame-001.jpg` through `ezgif-frame-232.jpg`

Run `node setup.mjs` or `setup.bat` to copy them from `/photos/`.
